{
  "buildCommand": "cd todo-app && rm -rf node_modules package-lock.json && npm install && npm run build",
  "outputDirectory": "todo-app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
} 